# PHP-CRUD-System-with-Modern-UI
A single-file script demonstrating Create, Read, Update, Delete (CRUD) operations on a MySQL database using native PHP and modern Bootstrap 5 styling. Requires a users table. Vulnerable to SQL injection (use prepared statements in production).
